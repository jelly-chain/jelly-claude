# firecrawl-skill

Convert any website to clean markdown — full-site crawling, structured data extraction, JS rendering.

## Install
```bash
bash install.sh
```

## Setup required
1. Get your API key (see below)
2. Add key to `~/.jelly-research/.keys`

## Required key
`FIRECRAWL_API_KEY — get one at firecrawl.dev`

## Example prompts
See full API examples in `SKILL.md`.
