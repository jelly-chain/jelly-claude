# exa-skill

Neural web search — semantic similarity search and full-page content crawling.

## Install
```bash
bash install.sh
```

## Setup required
1. Get your API key (see below)
2. Add key to `~/.jelly-research/.keys`

## Required key
`EXA_API_KEY — get one at exa.ai`

## Example prompts
See full API examples in `SKILL.md`.
