# replicate-skill

Run any of 1000+ AI models (Flux, SDXL, Llama, Whisper) via Replicate's prediction API.

## Install
```bash
bash install.sh
```

## Required key
`REPLICATE_API_TOKEN — get one at replicate.com`

## Key location
`~/.jelly-ai/.keys`

## Full API examples
See `SKILL.md` for complete TypeScript reference.
