// Phase 2: core init with minimal startup wiring (in-folder)
export async function initCore(){
  // placeholder for memory/context bootstrap and env load
  return { ok: true, started: new Date().toISOString() };
}
export default initCore;
